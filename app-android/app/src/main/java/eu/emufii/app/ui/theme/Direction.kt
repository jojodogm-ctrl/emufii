package eu.emufii.app.ui.theme

/**
 * DIRECTION CONTRACT — "HOME MENU"  (seed: brief-pinned, 2026-08-22)
 *
 * THESIS: Emufii is a handheld's home menu, not an app with a game list. It
 * refuses the translucent-glass dashboard it used to be — blurred panels, iOS
 * blue, drifting mesh gradients — because glass is what a phone app wears and
 * this thing is held like a console.
 *
 * OWN-WORLD: Moulded plastic under a tray light. A cool silver ground carrying a
 * fine engraved grid; white plastic plates with a hairline edge, a lit top bevel
 * and a real offset shadow; rounded-terminal type (Rounded M+); one signature
 * colour, tray cyan, spent only on the cursor and the primary action. Colour
 * otherwise comes from the box art. Dark is the same tray at night, never an
 * invert.
 *
 * STORY: the player sees their games as objects on a tray, finds the cursor
 * without hunting, presses A, and is gone into the emulator.
 *
 * FIRST VIEWPORT: the status strip across the top like a console's inset screen
 * — profile, tunnel, consoles — then the tray of square plates filling the rest,
 * the cursor lit on the first game.
 *
 * FORM: user-pinned world (the roll is beaten by a pinned brief); Operate mode.
 *
 * FINISH: unreviewed and undocumented is unfinished; this build ends with the
 * finish review, the verdict, and DESIGN.md.
 */
internal object Direction
