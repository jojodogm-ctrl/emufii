<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device, by either route.

  `cloud-backup` and `device-transfer` are refused for the same reason backup
  is off in the manifest: this app's private storage holds a VPN private key
  and a slice of the player's console keys. A transfer that carried them would
  be a copy of both onto a machine the player may not control.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
